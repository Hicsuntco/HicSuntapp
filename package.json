{
  "name": "hic-sunt",
  "version": "1.0.0",
  "private": true,
  "description": "Hic Sunt — cartographe de voyage sur-mesure",
  "scripts": {
    "ios:sync": "bash scripts/build-www.sh && npx cap sync ios",
    "ios:open": "npx cap open ios",
    "ios:assets": "npx @capacitor/assets generate --ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.4.1",
    "@capacitor/ios": "^8.4.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.1"
  }
}
